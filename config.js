const Config = {
  limit: 1000, //if the player's money is more than the one given here, he will receive a bag in his hand.
  // model: 'v_ret_ps_bag_01', //the object model name
  model: 'prop_ld_case_01', //the object model name
};
